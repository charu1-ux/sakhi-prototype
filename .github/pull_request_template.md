## Summary

<!-- 1–3 sentences describing what changed and why. -->

## Atomic-design impact

- [ ] Tokens (`src/styles/`) changed
- [ ] Primitives (`src/components/primitives/`) added/changed
- [ ] Atoms / molecules / organisms / templates added/changed
- [ ] None — purely app/page/data work

## Screenshots / Storybook

<!-- Attach light + dark mode shots, or a Storybook link. -->

## Checklist

- [ ] All styling is Tailwind utilities via `cn(...)` (no CSS modules / styled-components / `style={...}`)
- [ ] No raw hex/rgb/px — values come from tokens
- [ ] Storybook story added/updated for new components
- [ ] `npm run lint && npm run typecheck && npm run build` passes locally
- [ ] Conventional Commit message
