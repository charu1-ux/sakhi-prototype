/**
 * Project icon barrel. Re-exports a curated set of `lucide-react` icons plus any
 * locally generated SVGs from `./custom/`. Importing from `@/lib/icons` keeps
 * call-sites stable even if we swap the underlying icon set later.
 */

export {
  ArrowLeft,
  ArrowRight,
  ArrowUp,
  Check,
  ChevronDown,
  ChevronRight,
  Copy,
  Edit3,
  Loader2,
  Mic,
  Moon,
  Paperclip,
  Plus,
  Search,
  Send,
  Settings,
  Sparkles,
  Sun,
  X,
} from "lucide-react";

export type { LucideIcon } from "lucide-react";
